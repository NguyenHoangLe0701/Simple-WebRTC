spring.application.name=chatfacetimesmartdev

# Database Configuration

# spring.datasource.url=jdbc:mysql://localhost:3306/smart_chat_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=07012005

# Database (Render hoặc Aiven)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA Configuration
#dùng để update database
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
# dùng 8080 nếu SERVER_PORT không được set
server.port=8080

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

# Connection Pool
spring.datasource.hikari.maximum-pool-size=30
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

spring.main.allow-bean-definition-overriding=true

# CORS configuration
app.allowed.origins=http://localhost:5173,https://simple-web-rtc-two.vercel.app

# Gemini AI Configuration
gemini.api.key=${GEMINI_API_KEY}

# # CORS (optional: override allowed origins via env)
# app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://simple-web-rtc-two.vercel.app}

# # Redis Presence (enable via env and supply URL or host/port)
# # Enable Redis-backed presence store
# presence.redis.enabled=${PRESENCE_REDIS_ENABLED:false}

# # Option A: provide a single URL (recommended for Redis Cloud). Use rediss:// for TLS
# spring.data.redis.url=${SPRING_DATA_REDIS_URL:}

# # Option B: provide host/port/username/password separately (used if URL empty)
# spring.data.redis.host=${SPRING_DATA_REDIS_HOST:}
# spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
# spring.data.redis.username=${SPRING_DATA_REDIS_USERNAME:}
# spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:}
# spring.data.redis.ssl.enabled=${SPRING_DATA_REDIS_SSL_ENABLED:true}

# # STOMP Broker Relay (RabbitMQ/ActiveMQ via STOMP) - optional
# stomp.relay.enabled=${STOMP_RELAY_ENABLED:false}
# stomp.relay.host=${STOMP_RELAY_HOST:}
# stomp.relay.port=${STOMP_RELAY_PORT:61613}
# stomp.relay.login=${STOMP_RELAY_LOGIN:}
# stomp.relay.passcode=${STOMP_RELAY_PASSCODE:}


